"""Onboarding module (Module 1) — maps a society's physical structure.

Package layout mirrors the platform packages (docs/03 §2):
``models`` (tables) · ``schemas`` (Pydantic contracts) · ``numbering`` (pure
house-number engine) · ``repository`` (SQL only) · ``service`` (business logic +
audit, never commits) · ``router`` (thin HTTP) · ``spec`` (ModuleSpec + register).
"""
