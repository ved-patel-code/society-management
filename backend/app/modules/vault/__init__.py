"""Vault module — per-society document storage on MinIO (docs/modules/vault.md).

Admin-only file manager (nested folders, upload/preview/download, rename/move,
Trash) plus the physical home for cross-module files (ID proofs, complaint
images, notice attachments). Enforces a per-society storage quota.
"""
