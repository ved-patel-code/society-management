# Toggleable feature modules live here (one self-contained package each).
# EMPTY in the foundation build — the module framework exists and is exercised
# (see app/core/registry.py + app/platform/bootstrap.py), but no feature module
# is built yet. Adding one = new package + ModuleSpec + migration + permissions,
# with ZERO edits to existing modules (docs/02 §3).
