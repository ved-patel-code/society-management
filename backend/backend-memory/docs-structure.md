---
name: docs-structure
description: "Where the Society app's design docs live and how they're organized"
metadata: 
  node_type: memory
  type: project
  originSessionId: a92a120b-70b1-4de9-817d-84fa741891a8
---

Society app documentation lives in `d:\society\docs\`:
- `README.md` — index / start here
- `01-project-overview.md` — app summary, all modules, priority tiers
- `02-architecture.md` — stack, modular monolith, module framework (ModuleSpec + society_modules flags + two-gate access), data-driven roles, auth, multi-tenancy
- `03-backend-and-db-principles.md` — folder structure (module→feature→router/service/repository/schemas/models), `common/` shared helpers, backend-owns-all-logic + DB keeps only PK/FK/NOT NULL/UNIQUE, query efficiency, indexing
- `04-module-template.md` — fixed structure every module design doc follows. Includes a standard **§5 Audited actions** section (list of `audit_log` action keys + before/after captured) — every module doc must enumerate its audited actions. Onboarding/House-Occupancy/Vault already have it.
- `05-cross-module-contracts.md` — modules talk via service interfaces + skeleton-then-wire dependency map
- `modules/` — per-module DESIGN docs (written one at a time, before building)
- `implemented/` — per-module AS-BUILT docs. These are a LEAN NAVIGATION INDEX that points to code, NOT a copy of it. Per function: summary + depends-on + file location. Plus a file map, tables-owned list, cross-module wiring, and deviations. NO actual code, NO SQL/queries, NO endpoint schemas (find those in router.py / by file path). Goal: help an LLM find things fast with few tokens without duplicating/drifting from code.
- **Design docs stay detailed** (intent/rules/data model, written before code — not duplication). Only the as-built docs are lean indexes.
- **public_id is ON HOLD** — not implemented now (see [[modularity-model]]).

Docs 01–05 are self-contained: a fresh session reads them, then designs any single module without re-explaining the app.

- `platform/platform-foundation.md` — ✅ DESIGNED. The always-on foundation (auth, users, roles/permissions, societies, module allocation, tenant scoping). Must exist before any module runs. Key decisions: JWT access + revocable rotating DB refresh token; fine per-action permissions seeded from module registry; default roles copied per-society on creation; society default password REQUIRED at creation (hashed Argon2id); auto-provision/link owner accounts on occupancy; `revoke_house_access` (unlink + revoke sessions, deactivate if orphaned); data tied to `house_id` not login (occupant↔login via `house_occupancies.user_id`); EmailSender interface with terminal test mode. Deferred: tenant renter login/view, login brute-force protection, public_id, real SMTP.

**API URL conventions** (docs/02 §3.1): root-level, NO version prefix; scope-based prefixes (not per-role). Super-admin = `/admin/*` (society id in path); society-scoped modules = `/{module}/*` (society from JWT/TenantContext, no id in path); auth = `/auth/*`; `/me` for current user. society_admin & residents share module endpoints — permissions decide access, no per-role prefix.

- `modules/onboarding.md` — ✅ DESIGNED. Structure mapping. Key decisions: super_admin sets society NAME only, admin picks TYPE in onboarding (societies.type now NULLABLE); blocking resumable wizard (until complete, login shows only wizard; onboarding_progress table saves completed buildings + in-progress draft); house numbers unique PER BUILDING (partial unique indexes), identity = building+number, display A-201 (derived); numbering modes — building: auto (floor+zeropad(count,2), ground prefix default 'G'), sequential (per_building default | continuous), manual; individual: sequential (continuous across rows), custom (per-row prefix+count from 1), manual; per-building mode with prefill-repeat; building names typed by admin (no schemes); ONE shared houses table (onboarding owns structure cols, House&Occupancy owns status/occupancy); initial setup + basic later edits, deletes guarded if occupied/dues; every house created status=empty.

- `modules/house-occupancy.md` — ✅ DESIGNED. House status lifecycle + occupancy. Key decisions: transitions any non-empty↔non-empty, NEVER back to empty; required data per status (owned: owner name/email/phone/persons_living; to_let/for_sale: owner name/email/phone, identical except label; rented: owner + tenant{name,phone,persons_living,email optional}); ID proof OPTIONAL everywhere (image via Vault, wired later); owner data retained across statuses + editable; OWNER IDENTITY = EMAIL (same email=same owner keep access, changed email=replace owner → revoke old+provision new); owner email required (login), tenant login deferred; tables house_occupancies (party_type owner|tenant, user_id link, is_current, partial unique one-current-per-party) + house_status_history; first_left_empty_on set once → drives Finance; module key `houses` depends_on onboarding; shared houses table (this module owns status/occupancy cols).

- `modules/vault.md` — ✅ DESIGNED. File-manager document storage (MinIO), admin-only. Key decisions: unlimited nested folders; house-centric system layout `Houses/<house>/Proof` + `Houses/<house>/Complaints/<complaint>` (auto-created on first use, system-managed root, protected); Trash (soft-delete, auto-purge 30 days + manual empty, trashed bytes count toward quota until purged); allow all file types EXCEPT executable/dangerous denylist; NO per-file cap (reject if exceeds available storage); default 5 GB/society; inline preview PDF+images, others download; search deferred; backend-proxied upload (atomic type+quota); storage keyed by doc id (rename/move = DB only); tables vault_folders (tree, soft-delete, house_id link, system_key) + vault_documents + society_storage_usage; provides store_document/get_*_url/usage/ensure_house_folder; worker jobs trash-purge + usage-reconcile. NOTE: replaced old id_proofs/complaint_images system folders with house-centric layout (updated docs/05 + house-occupancy).

- `modules/finance.md` — ✅ DESIGNED. Society money. Key decisions: single society-wide EFFECTIVE-DATED rate (maintenance_rates); dues MATERIALIZED per house per calendar month by a worker on the society's maintenance_due_day (house_dues, idempotent, backfills from first_left_empty_on); houses with status!=empty owe; collection = whole-months OLDEST-FIRST, no partial-within-month, pay one/several/all (payments + payment_allocations); PREPAID 3/6/9/12 at LOCKED current rate, arrears cleared first, tied to house (prepaid_blocks materialize locked house_dues); reserve = COMPUTED RUNNING LEDGER (ledger_entries: opening/deposit/interest/resale_transfer/income/collection/expense/adjustment/reversal), admin posts entries anytime, reconcile posts adjustment; expenses with extendable predefined categories; corrections via VOID/REVERSAL (never delete) and voids/reversals stay VISIBLE in reports for transparency; full analytics incl. rate-change preview (projection); NO late fees/waivers (future); PaymentProvider interface (admin_manual now, gateway future); reminders delegated to modular Notifications (Finance emits due/overdue signals); module key `finance` depends_on houses.

- `modules/notifications.md` — ✅ DESIGNED. In-app notification + reminder ENGINE (reusable). Key decisions: IN-APP ONLY v1 (email/push future; `notify` keeps a channel seam); TWO paths — event-driven (modules emit domain events to in-process dispatcher → Notifications subscribers resolve recipients → `notify`) + scheduled reminder-rule REGISTRY (worker); v1 events → complaint.status_changed→`complaint_update`(raising owner), complaint.created→`complaint_new`(admins=holders of complaints.read_all), complaint.withdrawn→`complaint_withdrawn`(admins), notice_posted→`notice`(all current owners); DUES rule = advance (dues_advance_days, default 3) + due-day + every N (dues_reminder_interval_days, default 5) while unpaid, anchor on most-recent outstanding due_date, ONE consolidated `maintenance_due` per fire (sum all unpaid months), STATELESS/idempotent dedupe_key=`dues:{house}:{today}`, auto-stops when paid, consumes Finance outstanding_dues + maintenance_due_day (cadence config in Notifications NOT Finance); CLEAR-ON-READ — feed = read_at IS NULL, cleared when user opens notification OR source module calls `mark_read_for(user,entity_type,entity_id)` (Complaints on complaint-open, Notice Board on notice-open), read rows purged after read_retention_days (default 30); recipients data-driven (admins by permission, owners via current_owner_user_ids); NO opt-out/preferences v1; NOT audited except `notifications.config_updated`. One table `notifications` (society_id,user_id,type,title,body,payload jsonb,entity_type,entity_id,dedupe_key,read_at; partial UNIQUE(society_id,dedupe_key), idx(user_id,read_at)); NO prefs/deliveries tables; reminder rules in code. Perms notifications.read / notifications.configure. Endpoints GET /notifications(+unread-count), POST /{id}/read + /read-all, GET/PUT /config. depends_on finance (complaints/notices soft). Worker: daily dues scan + read-purge. WIRED complaints.md (emit created/withdrawn + mark_read_for), notice-board.md (mark_read_for), finance.md (dues rule hosted here), docs/05 (events table + flipped rows to ✅).

- `modules/notice-board.md` — ✅ DESIGNED. Society-wide broadcast board (admin posts, residents read; shared `/notices/*`, permissions decide). Key decisions: WHOLE-SOCIETY targeting only (society-scoped, no house_id); content = title + RICH TEXT body (sanitized server-side, anti-XSS) + UNLIMITED attachments (Vault-quota-bound); lifecycle status draft→published→withdrawn, EDIT after publish sets last_edited_at ("edited · date"), PIN (multiple), optional EXPIRY (`expires_at`, COMPUTED at query time — NO worker); active feed = published & not-expired & not-withdrawn, pinned-first then published_at DESC; ADMIN READ RECEIPTS (notice_reads per user, denominator = current owners via Occupancy `current_owner_user_ids`; reads NOT audited); ADMIN-ONLY archive (expired+withdrawn), residents see active feed only; emits `notice_posted` domain event for Notifications (skeleton, unconsumed); RESIDENT-PORTAL LANDING = active feed; perms notices.read / notices.publish / notices.read_receipts; tables notices + notice_attachments(vault_document_id) + notice_reads; module key `notices` depends_on houses; NO background jobs. VAULT EXTENDED: new `Notices/<notice id>/` society-level system folder (system_key notices_root|notice, notice_id link), vault_documents.source += `notice`, provider `ensure_notice_folder(notice)`.

- `modules/complaints.md` — ✅ DESIGNED. House-scoped resident complaints + admin workflow. Key decisions: shared `/complaints/*` endpoints, permissions decide access; status `open→in_progress→resolved→closed→archived` (admin-driven; admin MANUALLY closes resolved; worker AUTO-ARCHIVES after configurable `auto_archive_days`, default 15; admin correction reopen resolved→in_progress; resident withdraw only while open); categories PREDEFINED + admin-EXTENDABLE (complaint_categories, seed Plumbing/Electrical/Common Area/Security/Cleaning/Other, deactivate not delete) — COMMON-AREA IS A CATEGORY, complaint always ties to raiser's house (`house_id` NOT NULL, from owner occupancy; multi-house owner must pick); STATUS-ONLY + optional admin NOTE per transition (complaint_status_history timeline, no thread); resident EDIT+WITHDRAW while open then locked; images ≤2 report (resident) + ≤2 proof (admin), all optional, stored in VAULT under `Houses/<house>/Complaints/<reference>` (complaint_images.vault_document_id NOW WIRED); reference `C-000123` per-society; NO admin-raised / NO assignment v1; emits domain event `complaint.status_changed` for Notifications (skeleton, unconsumed no-op until built); module key `complaints` depends_on houses; worker daily auto-archive job.

**Handoff doc `d:\society\REMAINING-MODULES.md` is now FULLY CONSUMED — all modules designed.** It remains as a history of per-module requirements/decisions; the authoritative specs are the per-module docs under `docs/modules/`.

**Design order — ALL ✅ DONE:** Platform Foundation ✅ → Onboarding ✅ → House & Occupancy ✅ → Vault ✅ → Finance ✅ → Complaints ✅ → Notice Board ✅ → Notifications ✅. **Design phase complete; next phase = implementation (backend first).**

**Why:** User wants living design docs so adding/updating modules across separate sessions is easy without re-explaining architecture. Established 2026-07-04.
See [[modularity-model]] [[tech-stack]].
